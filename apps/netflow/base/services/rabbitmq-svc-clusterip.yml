# rabbitmq-svc-clusterip.yml
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-svc
  namespace: netflow
spec:
  type: ClusterIP
  selector:
    app: rabbitmq
  ports:
  - name: amqp
    port: 5672
    targetPort: 5672
